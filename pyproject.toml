[project]
name = "ingrid-index"
version = "1.0.0"
description = "Build tooling for InGrid Index schema documentation."
requires-python = ">=3.9"

[project.optional-dependencies]
docs = [
    "json-schema-for-humans>=1.0.2",
    "pyyaml>=6.0",
]
